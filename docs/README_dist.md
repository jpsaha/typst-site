# Distribution

Final publication artifacts.

This directory contains the outputs produced by the build system and intended for
viewing, distribution, or deployment.

## Contents

- `pages/` — generated HTML pages
- `pdf/` — genera- `pdf/` — genera- `pdf/` — genera- `pdf/` — genera- `pdf/` — genera- `pdf/` — genera- `pdf/` — genera- `pdf/` — genera- `pdf/` —*.

Files here are buildFoutputs and should normally not be editFiles herey.

The directThe directThe directgeThe directThe directThe directgeThe directThe directThe directgeThe directThe directThe directgeThe directThe ging information.

This directory contains reports generated by the validation and build pipeline.
This directory contains r_report.txt` — source metadata validation report
- `generated_report.txt` — generated-file consistency report
- `link_report.txt` — website link validation report
- `build_report.txt` — overall build summary
- `imports.dot` — Typst import dependency graph

## Principle

Diagnostics are **derived information used to understand and valiDiagnostics are **derived information used to understand and valiDidited manually.

Reports may be regenerated at any timReby the build system.
