//==============================================================
// operators.typ
//
// Frequently used mathematical operators.
//
// Usage:
//
//   $ #ker(T) $
//   $ #rank(A) $
//   $ #span(S) $
//
//==============================================================



//==============================================================
// Linear Algebra
//==============================================================

#let ker = math.op("Ker")
#let img = math.op("Im")
#let coker = math.op("Coker")
#let span = math.op("span")
#let rank = math.op("rank")
#let null = math.op("null")
#let tr = math.op("tr")
#let det = math.op("det")
#let adj = math.op("adj")
#let diag = math.op("diag")
#let dim = math.op("dim")
#let codim = math.op("codim")
#let char = math.op("char")
#let spec = math.op("Spec")

#let gl = math.op("GL")
#let sl = math.op("SL")
#let psl = math.op("PSL")
#let so = math.op("SO")
#let orth = math.op("O")
#let unitary = math.op("U")
#let su = math.op("SU")

#let hom = math.op("Hom")
#let end = math.op("End")
#let aut = math.op("Aut")
#let iso = math.op("Iso")



//==============================================================
// Set Theory
//==============================================================

#let card = math.op("card")
#let pow = math.op("Pow")



//==============================================================
// Analysis
//==============================================================

#let grad = math.op("grad")
#let div = math.op("div")
#let curl = math.op("curl")
#let lap = math.op("Δ")
#let hess = math.op("Hess")
#let jac = math.op("Jac")
#let res = math.op("Res")

#let vol = math.op("Vol")
#let area = math.op("Area")
#let length = math.op("Length")



//==============================================================
// Probability
//==============================================================

#let prob = math.op("Pr")
#let exp = math.op("E")
#let var = math.op("Var")
#let cov = math.op("Cov")
#let corr = math.op("Corr")

#let bin = math.op("Bin")
#let pois = math.op("Pois")



//==============================================================
// Number Theory
//==============================================================

#let gcd = math.op("gcd")
#let lcm = math.op("lcm")
#let ord = math.op("ord")
#let val = math.op("val")

#let charpoly = math.op("char")
#let minpoly = math.op("min")
#let disc = math.op("disc")
#let gal = math.op("Gal")
#let cl = math.op("Cl")
#let ann = math.op("Ann")



//==============================================================
// Algebra
//==============================================================

#let tor = math.op("Tor")
#let ext = math.op("Ext")

#let obj = math.op("Obj")
#let mor = math.op("Mor")
#let id = math.op("id")



//==============================================================
// Geometry
//==============================================================

#let conv = math.op("conv")
#let aff = math.op("aff")
#let interior = math.op("int")
#let boundary = math.op("bd")
#let closure = math.op("cl")



//==============================================================
// Graph Theory
//==============================================================

#let verts = math.op("V")
#let edges = math.op("E")
#let deg = math.op("deg")
#let diam = math.op("diam")
#let dist = math.op("dist")
#let girth = math.op("girth")



//==============================================================
// Combinatorics
//==============================================================

#let sym = math.op("Sym")
#let alt = math.op("Alt")
#let fix = math.op("Fix")
#let orb = math.op("Orb")
#let inv = math.op("Inv")



//==============================================================
// Miscellaneous
//==============================================================

#let supp = math.op("supp")
#let sgn = math.op("sgn")

#let re = math.op("Re")
#let im = math.op("Im")
#let arg = math.op("Arg")

#let log = math.op("Log")

#let max = math.op("max")
#let min = math.op("min")

#let sup = math.op("sup")
#let inf = math.op("inf")

#let lim = math.op("lim")
#let limsup = math.op("limsup")
#let liminf = math.op("liminf")

#let argmax = math.op("argmax")
#let argmin = math.op("argmin")